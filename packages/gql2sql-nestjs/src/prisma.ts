import * as P from '@prisma/client/runtime';

export namespace Prisma {
  export const join = P.join;
  export const sql = P.sqltag;
  export const empty = P.empty;
  export const raw = P.raw;
  export type Sql = P.Sql;
}
